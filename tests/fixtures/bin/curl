#!/usr/bin/env bash
#
# Stub para curl.
# Registra los argumentos recibidos y retorna un código configurable.

set -eu

printf '%q ' "$@" >>"${STUB_LOG:?STUB_LOG no definido}"
printf '\n' >>"${STUB_LOG:?STUB_LOG no definido}"

exit "${STUB_EXIT_CODE:-0}"
