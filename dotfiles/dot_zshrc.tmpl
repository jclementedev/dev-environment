# ~/.zshrc — gestionado por chezmoi. No editar manualmente.
# Los cambios deben realizarse en dotfiles/dot_zshrc.tmpl.

# -----------------------------------------------------------------------------
# PATH
# -----------------------------------------------------------------------------

typeset -U path PATH

path=(
  "$HOME/.local/bin"
  "$HOME/.cargo/bin"
  "${XDG_DATA_HOME:-$HOME/.local/share}/fnm"
  $path
)

[[ -d "$HOME/.opencode/bin" ]] && path=(
  "$HOME/.opencode/bin"
  $path
)

export PATH

# -----------------------------------------------------------------------------
# Herramientas del shell
# -----------------------------------------------------------------------------

# fnm: gestiona versiones de Node.js y cambia automáticamente según
# .node-version o .nvmrc cuando están presentes.
if command -v fnm >/dev/null 2>&1; then
  eval "$(fnm env --use-on-cd --shell zsh)"
fi

# fzf: atajos de teclado y completado difuso.
if command -v fzf >/dev/null 2>&1; then
  if fzf --zsh >/dev/null 2>&1; then
    source <(fzf --zsh)
  fi

  if command -v bat >/dev/null 2>&1; then
    _fzf_preview_cmd='bat --style=numbers --color=always --line-range :500'
  elif command -v batcat >/dev/null 2>&1; then
    _fzf_preview_cmd='batcat --style=numbers --color=always --line-range :500'
  else
    _fzf_preview_cmd='cat'
  fi

  export FZF_DEFAULT_COMMAND='rg --files --hidden --glob=!.git --glob=!node_modules --glob=!vendor --glob=!target 2>/dev/null'

  export FZF_DEFAULT_OPTS="
    --preview '${_fzf_preview_cmd} {} 2>/dev/null'
    --preview-window=right:65%:wrap:border-left
    --height=60%
    --layout=reverse
    --border=rounded
  "

  unset _fzf_preview_cmd
fi

# zoxide: navegación inteligente entre directorios.
if command -v zoxide >/dev/null 2>&1; then
  eval "$(zoxide init zsh)"
fi

# bat: visor de archivos con resaltado de sintaxis.
if command -v bat >/dev/null 2>&1; then
  export BAT_PAGER='less -RF'
elif command -v batcat >/dev/null 2>&1; then
  bat()
  {
    batcat "$@"
  }

  export BAT_PAGER='less -RF'
fi

# -----------------------------------------------------------------------------
# Aliases
# -----------------------------------------------------------------------------

# Carga alias compartidos y, opcionalmente, alias locales del usuario.
# El archivo de aliases locales es opcional y no está administrado por este repositorio.
readonly ZSH_CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/zsh"

if [[ -r "$ZSH_CONFIG_DIR/aliases.zsh" ]]; then
  source "$ZSH_CONFIG_DIR/aliases.zsh"
fi

if [[ -r "$ZSH_CONFIG_DIR/aliases.local.zsh" ]]; then
  source "$ZSH_CONFIG_DIR/aliases.local.zsh"
fi

# -----------------------------------------------------------------------------
# Editor
# -----------------------------------------------------------------------------

if command -v code >/dev/null 2>&1; then
  export EDITOR='code'
  export VISUAL='code'
else
  export EDITOR='vi'
  export VISUAL='vi'
fi

# -----------------------------------------------------------------------------
# Keybindings
# -----------------------------------------------------------------------------

bindkey -v
KEYTIMEOUT=1

# -----------------------------------------------------------------------------
# Prompt
# -----------------------------------------------------------------------------

if command -v starship >/dev/null 2>&1; then
  eval "$(starship init zsh)"
fi

# -----------------------------------------------------------------------------
# Plugins de Zsh
# -----------------------------------------------------------------------------

if [[ -f /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh ]]; then
  source /usr/share/zsh-autosuggestions/zsh-autosuggestions.zsh
fi

# Debe cargarse al final para envolver correctamente los widgets de ZLE.
if [[ -f /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ]]; then
  source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
fi
